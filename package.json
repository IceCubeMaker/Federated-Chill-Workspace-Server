{
  "name": "federated-chill-workspace",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "tsup": "^8.1.0",
    "vitest": "^1.6.0",
    "@types/node": "^20.14.0"
  },
  "pnpm": {
    "overrides": {}
  },
  "workspaces": [
    "packages/*"
  ]
}
